const pessoa = {
    nome: 'vitor',
    idade: 22
};

// TRANSFORMANDO ATRIBUTOS DE OBJETOS EM VARIÁVEIS

const { nome, idade } = pessoa;

// IGUAL A

// const nome = pessoa.nome;
// const idade = pessoa.idade;

console.log(nome, idade);