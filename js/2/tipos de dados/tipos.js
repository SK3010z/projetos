// Number (Número)
let idade = 25;

// String (Texto)
let nome = "João";

// Boolean (Booleano)
let isAdulto = true;

// Null (Nulo)
let valorNulo = null;  // AUSENCIA DE VALOR

// Undefined (Indefinido)
let valorIndefinido;  // AUSENCIA DE DEFINIÇÃO, é como se nem existisse

// Object (Objeto)
let pessoa = {
  nome: "Maria",
  idade: 30
};

// Array (Matriz)
let numeros = [1, 2, 3, 4, 5];

// Function (Função)
function somar(a, b) {
  return a + b;
}

// Exibindo alguns valores no console para verificação
console.log(idade);
console.log(nome);
console.log(isAdulto);
console.log(valorNulo);
console.log(valorIndefinido);
console.log(pessoa);
console.log(numeros);
console.log(somar(2, 3));
